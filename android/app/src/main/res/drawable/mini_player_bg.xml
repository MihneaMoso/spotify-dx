<?xml version="1.0" encoding="utf-8"?>
<!-- Floating mini-player container (Echo mini-player pill): surface fill,
     large house radius. The bar floats above the bottom nav with margins. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorSurface" />
    <corners android:radius="@dimen/echo_radius_large" />
</shape>
